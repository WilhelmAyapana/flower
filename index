<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Happy Valentine's</title>
  <style>
    body {
      background-color: #0a0a0a;
      text-align: center;
      font-family: Arial, sans-serif;
      overflow: hidden;
      position: relative;
      margin: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .message {
      font-size: 24px;
      font-weight: bold;
      color: #d63333;
      margin-bottom: 20px;
    }

    .garden {
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 400px;
    }

    .flower {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      animation: float 4s infinite ease-in-out;
    }

    .stem {
      width: 8px;
      height: 0;
      background: linear-gradient(to bottom, #206020, #103010);
      position: relative;
      border-radius: 5px;
      animation: growStem 3s ease-out forwards;
    }

    .petals-container {
      position: absolute;
      bottom: 200px;
      display: flex;
      gap: 50px;
      opacity: 0;
      animation: bloom 1.5s ease-in-out 3s forwards;
    }

    .petals {
      position: relative;
      width: 100px;
      height: 100px;
      opacity: 0;
      animation: bloom 1.5s ease-in-out 3.5s forwards;
    }

    .petal {
      width: 40px;
      height: 40px;
      background: radial-gradient(circle, #f6996b, #d50808);
      position: absolute;
      border-radius: 50%;
      box-shadow: 0 0 10px #ff8f8f;
      animation: pulse 2s infinite alternate;
    }

    .petal:nth-child(1) {
      top: 0;
      left: 30px;
      transform-origin: center bottom;
    }

    .petal:nth-child(2) {
      top: 30px;
      left: 0;
      transform-origin: right center;
    }

    .petal:nth-child(3) {
      top: 30px;
      right: 0;
      transform-origin: left center;
    }

    .petal:nth-child(4) {
      bottom: 0;
      left: 30px;
      transform-origin: center top;
    }

    .center {
      width: 30px;
      height: 30px;
      background: radial-gradient(circle, #ffcc33, #ff9900);
      border-radius: 50%;
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      box-shadow: 0 0 15px #ffcc33;
    }

    .leaves {
      position: absolute;
      bottom: 90px;
      /* Adjust so it sits correctly */
      width: 80px;
      height: 50px;
      background: linear-gradient(to right, #206020, #103010);
      border-radius: 50% 0 50% 0;
      opacity: 0;
      animation: growLeaves 2s ease-in-out 2.5s forwards;
      left: 50%;
      transform: translateX(-50%);
      /* Centers the leaves */
    }

    .leaves.left {
      transform: translateX(-50%) rotate(-30deg);
      /* Adjust rotation */
    }

    .leaves.right {
      transform: translateX(-50%) rotate(30deg);
      /* Adjust rotation */
    }

    .signature {
      position: absolute;
      bottom: 10px;
      left: 10px;
      font-size: 18px;
      font-weight: bold;
      color: #d63333;
    }

    @keyframes growStem {
      0% {
        height: 0;
      }

      100% {
        height: 200px;
      }
    }

    @keyframes bloom {
      0% {
        opacity: 0;
        transform: scale(0.5);
      }

      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    @keyframes growLeaves {
      0% {
        opacity: 0;
        transform: scale(0.5);
      }

      100% {
        opacity: 1;
        transform: scale(1);
      }
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
      }

      100% {
        transform: scale(1.1);
      }
    }

    @keyframes float {
      0% {
        transform: translateY(0);
      }

      50% {
        transform: translateY(-10px);
      }

      100% {
        transform: translateY(0);
      }
    }

    .stars {
      position: fixed;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      overflow: hidden;
    }

    .star {
      position: absolute;
      width: 4px;
      height: 4px;
      background: white;
      border-radius: 50%;
      box-shadow: 0 0 8px white;
      animation: twinkle 1.5s infinite alternate;
    }

    @keyframes twinkle {
      0% {
        opacity: 0.3;
        transform: scale(0.8);
      }

      100% {
        opacity: 1;
        transform: scale(1.3);
      }
    }
  </style>
</head>

<body>
  <h1 class="message">Happy Valentine's Day :)</h1>
  <div class="stars"></div>
  <div class="garden">
    <div class="flower">
      <div class="stem"></div>
      <div class="leaves left"></div>
      <div class="leaves right"></div>
      <div class="petals-container">
        <div class="petals">
          <div class="petal"></div>
          <div class="petal"></div>
          <div class="petal"></div>
          <div class="petal"></div>
          <div class="center"></div>
        </div>
      </div>
    </div>
  </div>
  <h1 class="signature">From: Wilhelm Ayapana</h2>
  <script>
    const starsContainer = document.querySelector(".stars");
    for (let i = 0; i < 200; i++) {
      let star = document.createElement("div");
      star.classList.add("star");
      star.style.top = Math.random() * 100 + "%";
      star.style.left = Math.random() * 100 + "%";
      star.style.animationDuration = Math.random() * 2 + 0.5 + "s";
      starsContainer.appendChild(star);
    }
  </script>
</body>

</html>
